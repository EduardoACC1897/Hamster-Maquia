using UnityEngine;

public class InventarioHamster : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    //[Header("Refencias UI")]
    //[SerializeField] private BarraEsenciaManager panelInventario;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
